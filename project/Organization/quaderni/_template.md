# Quaderno di laboratorio — [titolo breve del filone]

> Copia questo file in `project/Organization/quaderni/YYYY-MM-DD-<slug>.md` all’inizio di un filone di lavoro, oppure appendi una nuova sezione datata nello stesso file finché il filone resta aperto.

**Filone:** (es. refactor CSS fase 1, passo 8 ES modules)  
**Branch:** `cursor/...` o `master`  
**Agente genitore:** (es. css-specialist, refactor-lead)  
**Ultimo aggiornamento:** YYYY-MM-DD — (ruolo o subagent che ha scritto)

---

## Obiettivo

(Cosa deve essere vero a fine filone — una o due frasi misurabili.)

---

## Contesto minimo per il prossimo lavoratore

Il prossimo agente o subagent **non** ha memoria di questa chat. Deve poter partire leggendo solo:

| Cosa leggere | Path |
|--------------|------|
| Ruolo | `project/Organization/roles/...` |
| Specs rilevanti | `project/specs/...` |
| Test / gate | `project/specs/tests.md`, `project/tests/` |
| Organigramma | `project/Organization/organigramma.md` |
| Modello agentone | `project/Organization/modello-agentone.md` |

---

## Stato attuale

- [ ] (task non iniziato)
- [x] (task completato — descrizione breve)

**Commit rilevanti:** `abc1234` — messaggio  
**PR:** (link o numero, se esiste)

---

## Decisioni prese

| Data | Decisione | Motivo |
|------|-----------|--------|
| YYYY-MM-DD | … | … |

---

## Rischi / vincoli noti

- (es. classi `.mu_*` accoppiate a JS in `ExpressionManager.js`)
- (es. non toccare `preload.js` senza GOV)

---

## Handoff — prossimo passo

**Chi:** (genitore o subagent: es. Gabba gate smoke)  
**Cosa fare:** (istruzioni operative, non “come l’ultima volta”)  
**Definition of done:** (es. smoke PASS, nessun `!important` nuovo in `style.css`)

---

## Log sessioni (appendere sotto)

### YYYY-MM-DD — (agente)

- Fatto: …
- Risultato test: …
- Note per il successivo: …
